[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "mock_gpiozero"
version = "0.1.0"
description = "Mock gpiozero library for testing RPi behavior box code on non-RPi machines"
requires-python = ">=3.7"
